import React from "react";

interface StatusScreenProps {
    status: string;
}

export const StatusScreen: React.FC<StatusScreenProps> = ({status}) => {
    return (
        <div>
            
        </div>
    )
}